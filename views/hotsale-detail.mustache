<!DOCTYPE html>
<html lang="en">
  
    {{> template/head }}

  <body>
  
  {{> template/navbar }}

    <div class="site-blocks-cover-reduced site-blocks-cover  inner-page-cover overlay reduced-overlay" style="background-image: url(/images/hero_bg_2.jpg);" data-aos="fade" data-stellar-background-ratio="0.5">
      <div class="container">
        <div class="row align-items-center justify-content-center text-center">
          <div class="col-md-10">
            <span class="d-inline-block text-white px-3 mb-5 mt-3 property-offer-type rounded"> Detalles del Hotsale</span>
            <h1 class="mb-2">{{propiedad.direccion}} {{propiedad.ciudad}}, {{propiedad.provincia}}</h1>
          </div>
        </div>
      </div>
    </div>

    <div class="site-section site-section-sm">
      <div class="container">
        <div class="row">
          <div class="col-lg-12">
            <div>
              <div class="slide-one-item home-slider owl-carousel">
                <div><img src="{{propiedad.imagen}}" alt="Image" class="img-fluid"></div>
                <div><img src="{{propiedad.imagen}}" alt="Image" class="img-fluid"></div>
                <div><img src="{{propiedad.imagen}}" alt="Image" class="img-fluid"></div>
              </div>
            </div>
            <div class="bg-white property-body border-bottom border-left border-right">
              
              <div class="row mb-5">
                <div class="col-md-6 col-lg-4 text-center border-bottom border-top py-3">
                  <span class="d-inline-block text-black mb-0 caption-text">Tipo</span>
                  <strong class="d-block">{{propiedad.tipo}}</strong>
                </div>
                <div class="col-md-6 col-lg-4 text-center border-bottom border-top py-3">
                  <span class="d-inline-block text-black mb-0 caption-text">Ciudad</span>
                  <strong class="d-block">{{propiedad.ciudad}}</strong>
                </div>
                <div class="col-md-6 col-lg-4 text-center border-bottom border-top py-3">
                  <span class="d-inline-block text-black mb-0 caption-text">Provincia</span>
                  <strong class="d-block">{{propiedad.provincia}}</strong>
                </div>
              </div>
              <h2 class="h4 text-black">Descripcion</h2>
              <p>{{propiedad.descripcion}}</p>
              <h2 class="h4 text-black">Detalles del hotsale</h2>
              <div class="container">
                    <h4 class="mt-3">Semana de alojamiento: {{periodo}}</h4>                
              </div>
              <div class="container">
                    <h4 class="mt-3">Precio de la propiedad: {{semana.hotsale.monto}}</h4>                
              </div>
              <form class="form-group col-4" method="POST">
                {{#usuario.tipo}}
                    <button type="submit" class="btn btn-success my-3" formaction="/reserve-hotsale/{{propiedad._id}}/{{semana.numeroSemana}}">Comprar</button>
                {{/usuario.tipo}}
                {{^usuario.tipo}}
                    <label class="mt-3" for="sel1">Debes ser usuario premium para alquilar una propiedad de forma directa</label>
                    <button type="submit" disabled class="btn btn-success my-3">Reservar</button>
                {{/usuario.tipo}}
              </form>

            </div>
          </div>  
        </div>
        
      </div>
    </div>

    {{> template/footer }}

  </div>

  {{> template/scripts }}
    
  </body>
</html>